# Supabase MCP Server

这是一个 Model Context Protocol (MCP) 服务器，允许你通过自然语言与 Supabase 数据库进行交互。

## 功能特性

- ✅ **查询数据** (`supabase_query`) - 支持过滤、排序、分页
- ✅ **插入数据** (`supabase_insert`) - 支持单条或多条记录
- ✅ **更新数据** (`supabase_update`) - 支持批量更新
- ✅ **删除数据** (`supabase_delete`) - 支持条件删除
- ✅ **RPC 调用** (`supabase_rpc`) - 调用数据库函数
- ✅ **列出表** (`supabase_list_tables`) - 查看所有表
- ✅ **获取表结构** (`supabase_get_table_schema`) - 查看表结构信息

---

## 快速开始（5 分钟设置）

### 1. 安装和构建（2 分钟）

```bash
npm install
npm run build
```

### 2. 获取配置信息（1 分钟）

1. 打开 [Supabase Dashboard](https://app.supabase.com)
2. 选择你的项目
3. Settings → API
4. 复制 **Service Role Key**（secret key）
5. 复制 **Project URL**

### 3. 配置 Cursor（1 分钟）

在项目根目录创建 `.cursor/mcp.json`：

```json
{
  "mcpServers": {
    "supabase": {
      "command": "node",
      "args": ["你的绝对路径/dist/server.js"],
      "env": {
        "NEXT_PUBLIC_SUPABASE_URL": "你的项目URL",
        "SUPABASE_SECRET_KEY": "你的Service Role Key"
      }
    }
  }
}
```

**获取绝对路径：**
- Windows PowerShell: `(Get-Location).Path`
- macOS/Linux: `pwd`

然后拼接 `/dist/server.js`

### 4. 重启 Cursor（1 分钟）

完全关闭并重新打开 Cursor。

### 5. 测试

在 Cursor 中输入：

```
列出数据库中的所有表
```

如果 AI 能够执行，说明配置成功！🎉

---

## 详细安装指南

### 安装依赖

```bash
npm install
```

### 构建项目

```bash
npm run build
```

### 配置环境变量

在 Cursor 的 MCP 配置文件中设置环境变量。配置文件位置：
- Windows: `%APPDATA%\Cursor\User\globalStorage\saoudrizwan.claude-dev\settings\cline_mcp_settings.json`
- macOS: `~/Library/Application Support/Cursor/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`
- Linux: `~/.config/Cursor/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`

或者直接在项目根目录创建 `.cursor/mcp.json` 文件：

```json
{
  "mcpServers": {
    "supabase": {
      "command": "node",
      "args": ["你的绝对路径/dist/server.js"],
      "env": {
        "NEXT_PUBLIC_SUPABASE_URL": "https://your-project.supabase.co",
        "SUPABASE_SECRET_KEY": "your-service-role-key-here"
      }
    }
  }
}
```

**重要提示：**
- 将路径替换为你的实际项目路径
- 将 `your-service-role-key-here` 替换为你的 Supabase Service Role Key
- Service Role Key 可以在 Supabase Dashboard → Settings → API → Service Role Key 找到

### 验证安装

重启后，你可以在 Cursor 中尝试以下自然语言命令：

```
列出数据库中的所有表
```

```
查询 profiles 表中的前 10 条记录
```

如果 MCP 服务器正常工作，AI 助手应该能够理解并执行这些命令。

### 故障排除

#### 问题：MCP 服务器无法启动

**解决方案：**
1. 确保已运行 `npm run build`
2. 检查路径是否正确（使用绝对路径）
3. 检查环境变量是否正确设置
4. 查看 Cursor 的控制台日志

#### 问题：权限错误

**解决方案：**
- 确保使用 Service Role Key 而不是 Anon Key
- Service Role Key 具有完全访问权限，可以绕过 RLS（Row Level Security）

#### 问题：找不到模块

**解决方案：**
```bash
npm install
npm run build
```

### 开发模式

如果你想在开发时实时测试更改：

```bash
npm run dev
```

这将使用 `tsx` 直接运行 TypeScript 文件，无需每次构建。

---

## 使用示例

配置完成后，你可以在 Cursor 中使用自然语言与 Supabase 交互：

### 查询数据

#### 基本查询

```
查询 profiles 表中的所有记录
```

```
显示 profiles 表中的前 10 条记录
```

```
查询 profiles 表，只返回 id、username 和 credits 字段
```

#### 条件查询

```
查找 profiles 表中 credits 大于 100 的用户
```

```
查询 profiles 表中 email 为 "test@example.com" 的用户
```

```
查找 profiles 表中 username 包含 "张" 的用户
```

```
查询 profiles 表中 created_at 在最近 7 天内的记录
```

#### 排序和分页

```
查询 profiles 表，按 created_at 倒序排列，返回前 20 条
```

```
查询 profiles 表，按 credits 降序排列，跳过前 10 条，返回接下来的 10 条
```

#### 复杂查询

```
查询 profiles 表中 credits 大于 50 且 email 不为空的用户，按 created_at 倒序排列
```

```
查找 profiles 表中 id 在 [1, 2, 3, 4, 5] 中的用户
```

### 插入数据

#### 单条记录

```
向 profiles 表插入一条新记录：username 为 "张三"，email 为 "zhangsan@example.com"，credits 为 100
```

```
在 tasks 表中创建一条新任务：user_id 为 123，prompt 为 "生成一张美丽的风景图"，status 为 "pending"
```

#### 多条记录

```
向 profiles 表插入多条记录：
- username: "李四", email: "lisi@example.com", credits: 50
- username: "王五", email: "wangwu@example.com", credits: 200
```

### 更新数据

#### 单条更新

```
更新 profiles 表中 id 为 1 的记录，将 credits 设置为 200
```

```
更新 profiles 表中 email 为 "test@example.com" 的用户，将 username 改为 "新名字"，credits 增加 50
```

#### 批量更新

```
更新 profiles 表中 credits 小于 10 的所有用户，将 credits 设置为 10
```

```
将所有 status 为 "pending" 的任务更新为 "processing"
```

### 删除数据

#### 条件删除

```
删除 profiles 表中 id 为 123 的记录
```

```
删除 profiles 表中 email 为 "test@example.com" 的记录
```

```
删除 tasks 表中 created_at 早于 2024-01-01 的所有记录
```

### RPC 函数调用

#### 调用数据库函数

```
调用 deduct_credits_and_create_task 函数，参数为：
- p_user_id: 123
- p_prompt: "生成一张图片"
- p_credits_to_deduct: 10
```

```
执行 get_user_stats 函数，传入 user_id 为 456
```

### 表管理

#### 查看表列表

```
列出数据库中的所有表
```

```
显示所有可用的表名
```

#### 查看表结构

```
查看 profiles 表的结构
```

```
显示 tasks 表的列信息和数据类型
```

### 实际业务场景示例

#### 用户管理

```
查询所有积分大于 100 的用户，按积分降序排列
```

```
给所有积分不足 10 的用户增加 50 积分
```

```
查找最近 30 天内注册的用户数量
```

#### 任务管理

```
查询所有状态为 "pending" 的任务
```

```
将任务 ID 为 789 的状态更新为 "completed"
```

```
删除所有超过 7 天的已完成任务
```

#### 数据分析

```
统计每个用户的积分总数
```

```
查询今天创建的任务数量
```

```
找出积分最高的前 10 名用户
```

---

## 工具详细说明

### supabase_query

查询数据库表，支持复杂的过滤和排序。

**参数：**
- `table` (必需) - 表名
- `select` (可选) - 要选择的列，默认为 "*"
- `filters` (可选) - 过滤条件对象
- `orderBy` (可选) - 排序字段，格式：`column.desc` 或 `column.asc`
- `limit` (可选) - 返回结果数量，默认 100
- `offset` (可选) - 分页偏移量，默认 0

**过滤条件示例：**
```json
{
  "filters": {
    "name": { "operator": "eq", "value": "John" },
    "age": { "operator": "gte", "value": 18 }
  }
}
```

支持的运算符：`eq`, `neq`, `gt`, `gte`, `lt`, `lte`, `like`, `ilike`, `in`, `is`

### supabase_insert

插入新记录。

**参数：**
- `table` (必需) - 表名
- `data` (必需) - 数据对象或数组

### supabase_update

更新记录。

**参数：**
- `table` (必需) - 表名
- `filters` (必需) - 过滤条件
- `data` (必需) - 要更新的字段和值

### supabase_delete

删除记录。

**参数：**
- `table` (必需) - 表名
- `filters` (必需) - 过滤条件

### supabase_rpc

调用数据库 RPC 函数。

**参数：**
- `functionName` (必需) - 函数名
- `params` (可选) - 函数参数对象

### supabase_list_tables

列出数据库中的所有表。

### supabase_get_table_schema

获取指定表的结构信息（列名、类型等）。

**参数：**
- `table` (必需) - 表名

---

## 项目结构

```
mcp-supabase/
├── src/
│   └── server.ts          # MCP 服务器主文件
├── dist/                  # 编译后的文件（运行 npm run build 后生成）
├── package.json           # 项目依赖配置
├── tsconfig.json          # TypeScript 配置
├── .gitignore            # Git 忽略文件
└── README.md             # 本文档
```

## 技术栈

- **TypeScript** - 类型安全
- **@modelcontextprotocol/sdk** - MCP 协议实现
- **@supabase/supabase-js** - Supabase 客户端

## 配置要求

- **环境变量**：
  - `NEXT_PUBLIC_SUPABASE_URL` 或 `SUPABASE_URL`
  - `SUPABASE_SECRET_KEY` 或 `SUPABASE_SERVICE_ROLE_KEY`

- **权限**：需要 Service Role Key（完全访问权限）

## 开发

```bash
# 开发模式（使用 tsx）
npm run dev

# 构建
npm run build

# 运行构建后的文件
npm start
```

---

## 安全注意事项

⚠️ **重要**：此 MCP 服务器使用 Service Role Key，具有完全访问权限。请确保：

1. **不要提交 Service Role Key 到 Git**
   - Service Role Key 具有完全访问权限
   - 应该存储在环境变量中，而不是代码中

2. **限制访问**
   - 仅在受信任的环境中使用
   - 考虑实现额外的权限检查

3. **使用环境变量**
   - 不要在配置文件中硬编码密钥
   - 使用环境变量或密钥管理服务

4. **谨慎操作**
   - 在生产环境中谨慎使用删除和更新操作
   - 在执行批量操作前，先查询确认

## 注意事项

1. **自然语言描述**：尽量用自然语言描述你的需求，AI 会自动转换为相应的工具调用

2. **表名和字段名**：使用实际的表名和字段名，AI 会直接使用这些名称

3. **数据类型**：确保提供的数据类型与数据库字段类型匹配

4. **权限**：Service Role Key 具有完全访问权限，可以绕过 RLS（Row Level Security）

5. **错误处理**：如果遇到错误，AI 会返回详细的错误信息。常见错误：
   - **表不存在**：检查表名是否正确
   - **字段不存在**：检查字段名是否正确
   - **数据类型不匹配**：确保提供的数据类型正确
   - **权限错误**：确保使用 Service Role Key

## 提示

- 使用具体的表名和字段名可以获得更准确的结果
- 对于复杂查询，可以分步骤进行
- 在删除或更新大量数据前，先查询确认
- 使用 RPC 函数可以执行复杂的业务逻辑

## 许可证

MIT
